# Install/runtime state (created by install.ps1, never source-controlled)
.agentic-state/
*.lock
.install-lock
.install-journal/

# Fixture temp dirs (created by run-*-tests.ps1)
fixtures/_tmp/
fixtures/.run/

# Editor / OS
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store

# Build artifacts (no build step, but defensive)
dist/
build/
*.log
