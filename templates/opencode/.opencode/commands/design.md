# Design

Use the `design` skill to create or update a design or plan document under `{{game_docs_root}}/`.

Requirements:

1. Check existing `{{game_docs_root}}/` plans and designs before creating a new document.
2. Follow `.opencode/skills/design/SKILL.md`.
3. Route review to `architecture-reviewer` and/or `radical-design-critic` when the document is ready for review.
4. Keep Perforce status accurate after file changes.
